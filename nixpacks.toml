# nixpacks.toml
# Configure system libraries and package installation overrides.

[phases.setup]
nixLibs = [
    "xorg.libxcb",
    "libGL",
    "glib",
    "xorg.libX11",
    "xorg.libXext",
    "xorg.libXrender"
]

[phases.install]
cmds = [
    "python -m venv .venv",
    ".venv/bin/pip install --upgrade pip",
    ".venv/bin/pip install -r requirements.txt",
    ".venv/bin/pip uninstall -y opencv-python opencv-python-headless",
    ".venv/bin/pip install opencv-python-headless"
]
